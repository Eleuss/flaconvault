# ---- apps/verify (FastAPI) ----
FV_MASTER_KEY=00000000000000000000000000000000   # Hackathon: factory key, 16 byte hex
FV_KEY_MODE=factory                              # factory | diversified (AN10922)
FV_SERVER_ED25519_SEED=                          # 32 byte hex — generate: python -c "import os;print(os.urandom(32).hex())"
FV_SERVER_KEY_ID=1
FV_DEV_SIMULATOR=true                            # until hardware arrives
FV_NONCE_TTL_S=90
FV_IRYS_KEY=                                     # devnet keypair json (week 2)
FV_SOLANA_RPC=https://api.devnet.solana.com
FV_PROGRAM_ID=                                   # after deploy
FV_DB_PATH=./data/flaconvault.db
FV_WEB_URL=http://localhost:3000                 # where /t redirects to
FV_VERIFIER_HOST=http://localhost:8787           # host baked into tag URLs
FV_CORS_ORIGINS=http://localhost:3000

# ---- apps/web (Next.js) ----
NEXT_PUBLIC_VERIFY_URL=http://localhost:8787
NEXT_PUBLIC_SOLANA_RPC=https://api.devnet.solana.com
NEXT_PUBLIC_PROGRAM_ID=
NEXT_PUBLIC_DEV_SIMULATOR=true
